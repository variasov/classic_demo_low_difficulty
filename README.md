# Demo with low difficulty

This is a demo of "Classic" libs with Clean Architecture on low difficulty.

Part of project "Classic".

For start need:

```DB_URL=/tmp/demo.sqlite gunicorn dashboard.composites.api:app```
